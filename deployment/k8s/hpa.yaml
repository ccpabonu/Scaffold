apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: pruebagcp-app-hpa
  namespace: pruebas
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: pruebagcp-app
  minReplicas: 2
  maxReplicas: 5
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 50

